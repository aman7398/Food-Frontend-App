pending:
    put go back buttons
    show message on success requests
    User profile page
    login/logout navigation

ensure the baseUrl in app/api/api/api.js

onDeploy:
    replace the fallback pincode
    replace the BASE URL
